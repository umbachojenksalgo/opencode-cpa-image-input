interface:
  display_name: "OpenCode CPA 图片输入排障"
  short_description: "排查 OpenCode 自定义 CPA 提供商的图片输入问题"
  default_prompt: "Use $opencode-cpa-image-input to fix OpenCode 自定义 CPA 提供商的图片输入失败、模型缓存过旧和鉴权配置不匹配问题。"

policy:
  allow_implicit_invocation: true
